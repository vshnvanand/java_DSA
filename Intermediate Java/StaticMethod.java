public class StaticMethod {
    // something that has to be accessd from the class directly and not from the instance/object is called static
    
    // singleton pattern when just only one object has to made throught out the application of a specific class then we use singleton pattern 
}
